{
  "private": true,
  "name": "specs",
  "workspaces": ["packages/schema", "packages/cli"],
  "scripts": {
    "build": "npm run build --workspaces",
    "test": "vitest run"
  },
  "devDependencies": {
    "vitest": "^3.1.1",
    "typescript": "^5.3.3"
  }
}
