{
  "private": true,
  "name": "specs",
  "workspaces": [
    "packages/schema",
    "packages/cli"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "vitest": "^3.1.1"
  },
  "dependencies": {
    "@directededges/specs-from-figma": "file:../specs-from-figma"
  }
}
